// Example:
var scores = [30, 95, 90, 50, 20, 10];

scores.sort(function(a,b) {return a-b});
document.write(scores);

scores.reverse();
document.write(scores);

// Exersize:
var arr = [];
for (i=11; i<=23; i++) {
    arr.push(i);
}
document.write(arr);
